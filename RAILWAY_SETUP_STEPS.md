# 🚀 Пошаговая настройка Railway

## Шаг 1: Настройка GitHub App

1. **Нажмите "Configure GitHub App"** (кнопка с иконкой шестеренки)
2. Railway откроет окно авторизации GitHub
3. **Разрешите доступ** к вашим репозиториям:
   - Выберите "All repositories" (все репозитории)
   - ИЛИ выберите только `benefideal-store`
4. Нажмите "Install" или "Authorize"

## Шаг 2: Выбор репозитория

После авторизации:
1. Вернетесь на страницу Railway
2. В поле "What would you like to deploy today?" начните вводить `benefideal-store`
3. Выберите ваш репозиторий из списка
4. Нажмите "Deploy"

## Шаг 3: Ожидание деплоя

Railway автоматически:
- ✅ Определит, что это Node.js проект
- ✅ Установит зависимости (`npm install`)
- ✅ Запустит сервер (`node server.js`)
- ✅ Создаст URL для вашего приложения

**Время:** 2-3 минуты

## Шаг 4: Настройка переменных окружения

После успешного деплоя:

1. В Railway → ваш проект → **Variables** (или **Settings** → **Variables**)
2. Добавьте переменные:

```
CARDLINK_SHOP_ID = oa2Q9jq2Al
CARDLINK_API_TOKEN = (ваш токен из CardLink)
TELEGRAM_BOT_TOKEN = 8460494431:AAFOmSEPrzQ1j4_L-4vBG_c38iL2rfx41us
TELEGRAM_CHAT_ID = 8334777900
ADMIN_PASSWORD = (ваш пароль для админ-панели)
```

3. После добавления переменных Railway **автоматически перезапустит** сервер

## Шаг 5: Получить URL приложения

1. В Railway → ваш проект → **Settings** → **Domains**
2. Запишите Railway URL (например: `benefideal-store-production.up.railway.app`)
3. ИЛИ добавьте свой домен `benefideal.ru`:
   - Нажмите "Custom Domain"
   - Введите `benefideal.ru`
   - Обновите DNS записи у регистратора домена

## Шаг 6: Обновить Callback URL в CardLink

⚠️ **КРИТИЧЕСКИ ВАЖНО!**

1. Зайдите в CardLink → проект "Benefideal"
2. Найдите "Result URL" или "Callback URL"
3. Измените на:
   - `https://ВАШ_RAILWAY_URL.up.railway.app/api/cardlink/callback`
   - ИЛИ если используете свой домен: `https://benefideal.ru/api/cardlink/callback`
4. Сохраните изменения

## Шаг 7: Проверка

1. Откройте сайт: `https://ВАШ_RAILWAY_URL.up.railway.app`
2. Проверьте health: `https://ВАШ_RAILWAY_URL.up.railway.app/health`
   - Должно вернуть: `{"status":"ok"}`
3. Сделайте тестовый заказ
4. Проверьте Telegram — должно прийти уведомление
5. Проверьте админ-панель: `https://ВАШ_RAILWAY_URL.up.railway.app/admin.html?password=ВАШ_ПАРОЛЬ`

---

## ❓ Проблемы?

### "No repositories found"
- Убедитесь, что репозиторий `benefideal-store` существует на GitHub
- Проверьте, что вы авторизовали Railway в GitHub

### "Build failed"
- Проверьте логи: Railway → Deployments → последний деплой → Logs
- Убедитесь, что `package.json` есть в репозитории

### "Cannot find module"
- Проверьте, что все зависимости указаны в `package.json`
- Railway автоматически выполнит `npm install`

---

## ✅ Готово!

После выполнения всех шагов ваш сайт будет работать на Railway без спин-дауна! 🎉

