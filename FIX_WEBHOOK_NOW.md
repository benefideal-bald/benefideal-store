# 🔧 СРОЧНО: Настройка Telegram Webhook

## ⚠️ ПРОБЛЕМА: Кнопка "Ответить" не работает, потому что webhook не настроен!

## Шаг 1: Найдите ваш Railway URL

1. Откройте [Railway.app](https://railway.app)
2. Войдите в аккаунт
3. Найдите проект `benefideal-store`
4. Кликните на проект
5. В левом меню выберите **"Settings"**
6. Прокрутите до раздела **"Domains"**
7. **Скопируйте ваш URL** (например: `benefideal-store-production.up.railway.app`)

**ИЛИ** если у вас настроен свой домен:
- Используйте `benefideal.ru`

---

## Шаг 2: Настройте Webhook

Откройте в браузере (замените `ВАШ_URL` на ваш реальный URL):

```
https://ВАШ_URL/api/telegram/set-webhook
```

**Примеры:**
- Если Railway URL: `https://benefideal-store-production.up.railway.app/api/telegram/set-webhook`
- Если свой домен: `https://benefideal.ru/api/telegram/set-webhook`

**Вы должны увидеть:**
```json
{
  "success": true,
  "message": "Webhook установлен",
  "webhookUrl": "https://ваш-url/api/telegram/webhook"
}
```

---

## Шаг 3: Проверьте статус Webhook

Откройте в браузере:

```
https://ВАШ_URL/api/telegram/webhook-status
```

**Должно быть:**
```json
{
  "success": true,
  "webhookInfo": {
    "ok": true,
    "result": {
      "url": "https://ваш-url/api/telegram/webhook",
      "has_custom_certificate": false,
      "pending_update_count": 0
    }
  }
}
```

**Если `url` пустой** - webhook НЕ настроен! Повторите Шаг 2.

---

## Шаг 4: Проверьте работу

1. Отправьте тестовое сообщение через чат на сайте
2. В Telegram придет сообщение с кнопкой "💬 Ответить"
3. **Нажмите на кнопку**
4. Должно сразу прийти: "💬 Введите ваше сообщение:"
5. Напишите любое сообщение - оно отправится клиенту

---

## ❌ Если не работает:

1. **Проверьте логи Railway:**
   - Railway → Deployments → последний деплой → Logs
   - При нажатии на кнопку должны появиться логи:
     - `📥 Telegram webhook received`
     - `🔘 Callback query received`
     - `✅ Callback query answered`
     - `✅ Reply prompt sent`

2. **Если логов НЕТ:**
   - Webhook не настроен или не работает
   - Повторите Шаг 2

3. **Если логи ЕСТЬ, но сообщение не приходит:**
   - Проверьте, что `BOT_TOKEN` правильный в Railway → Variables
   - Проверьте, что сервер доступен по HTTPS

---

## 🎯 ГЛАВНОЕ:

**НЕ используйте `ваш-домен.com` - это пример!**

Используйте ваш **реальный Railway URL** или **benefideal.ru**

