# 🛡️ Защита отзывов от пропадания

## Проблема

Отзывы клиентов периодически пропадают из базы данных. Это **критически важная проблема**, так как:
- Отзывы - это ценная информация от клиентов
- Отзывы не должны пропадать
- Невозможно каждый день восстанавливать отзывы вручную

## Причины пропадания отзывов

### 1. База данных пересоздается при деплое (Render Free план)
- При каждом деплое Render может пересоздавать контейнер
- Файловая система может быть не полностью persistent
- База данных может быть потеряна, если файл не сохранился

### 2. Проблемы с сохранением на диск
- SQLite может не синхронизировать данные сразу
- WAL checkpoint может не работать правильно
- Данные могут быть потеряны при перезапуске

### 3. Ошибки в коде
- Нет явного удаления отзывов в коде (проверено)
- Но база данных может пересоздаваться

## Решение

### 1. Автоматическое восстановление при каждом запуске
- При каждом запуске сервера проверяются критические отзывы (Тихон, Илья)
- Если отзыв отсутствует - автоматически создается/восстанавливается
- Используется `CURRENT_TIMESTAMP` для обеспечения новизны

### 2. Проверка размера базы данных
- При запуске проверяется размер файла базы данных
- Если база очень маленькая (< 1000 байт) - предупреждение в логах

### 3. Детальное логирование
- Все операции с отзывами логируются
- Предупреждения о пропавших отзывах
- Критические ошибки выделяются в логах

### 4. WAL Checkpoint
- После каждого сохранения отзыва вызывается `PRAGMA wal_checkpoint(FULL)`
- Гарантирует немедленную синхронизацию с диском

### 5. Защита от перезаписи
- Используется `INSERT OR IGNORE` для статических отзывов
- Проверка существования перед вставкой
- Обновление `created_at` вместо пересоздания

## Что нужно сделать

### Краткосрочное решение (уже реализовано):
1. ✅ Автоматическое восстановление при запуске
2. ✅ Проверка наличия отзывов
3. ✅ Детальное логирование
4. ✅ WAL checkpoint после каждого сохранения

### Долгосрочное решение (рекомендуется):

#### Вариант 1: Использовать Render Disk (платно)
- Render Disk - это постоянное хранилище
- База данных будет сохраняться между деплоями
- Стоимость: ~$0.25/GB в месяц

#### Вариант 2: Использовать внешнюю базу данных
- PostgreSQL на Render (платно) или другой провайдер
- База данных будет отделена от приложения
- Гарантированная сохранность данных

#### Вариант 3: Резервное копирование
- Настроить автоматическое резервное копирование базы данных
- Сохранение в S3 или другой облачный сервис
- Восстановление при потере данных

#### Вариант 4: Использовать другой хостинг
- Railway (более надежное хранилище на Free плане)
- Heroku (платно, но надежно)
- VPS с собственным сервером

## Текущая защита

### Автоматическое восстановление:
- **Тихон**: Восстанавливается при каждом запуске, если отсутствует
- **Илья**: Восстанавливается при каждом запуске, если отсутствует
- **Статические отзывы**: Восстанавливаются, если таблица пустая

### Проверки:
- Количество отзывов при запуске
- Размер файла базы данных
- Наличие критических отзывов (Тихон, Илья)

### Логирование:
- Все операции логируются
- Критические проблемы выделяются
- Предупреждения о пропавших отзывах

## Мониторинг

### Проверка в логах Render:
1. Откройте Render Dashboard → Logs
2. Ищите сообщения:
   - `✅ Reviews table has X reviews - all safe!` - все хорошо
   - `🚨🚨🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА: Reviews table is EMPTY!` - проблема!
   - `⚠️ WARNING: Тихон review is MISSING!` - отзыв пропал
   - `✅ Тихон review AUTO-RESTORED successfully` - отзыв восстановлен

### Проверка через API:
- `GET /api/debug/check-all-reviews` - проверить все отзывы
- `GET /api/debug/find-tikhon` - найти отзывы Тихона
- `GET /api/debug/restore-tikhon` - восстановить отзыв Тихона

## Рекомендации

1. **Регулярно проверяйте логи Render** - следите за предупреждениями
2. **Используйте Render Disk или внешнюю базу данных** - для гарантированной сохранности
3. **Настройте резервное копирование** - для дополнительной защиты
4. **Мониторьте количество отзывов** - если количество резко упало - это проблема

## Важно

- Отзывы НЕ должны пропадать
- Автоматическое восстановление работает, но это не идеальное решение
- Для гарантированной сохранности используйте постоянное хранилище (Render Disk) или внешнюю базу данных

