# 💰 Что нужно для работы платежей

## 📋 Краткий список

Для того, чтобы деньги приходили на ваш счет, нужно настроить:

### 1. **Переменные окружения** (обязательно)
### 2. **Webhook URL** в личном кабинете платежной системы (обязательно)
### 3. **Доступность сервера** из интернета (обязательно)

---

## 🔧 Настройка CardLink (основная система)

### Шаг 1: Получите данные из личного кабинета CardLink

1. Зайдите на https://cardlink.link
2. Войдите в свой аккаунт
3. Откройте проект "Benefideal"
4. Перейдите в раздел **"API integration"**

**Что нужно скопировать:**

- **Shop ID** (`oa2Q9jq2Al` - уже виден на странице)
  - Нажмите на иконку копирования рядом с полем
  
- **API Token**
  - Прокрутите до раздела "Tokens"
  - Нажмите **"+ Create"**
  - Введите имя (например, "Benefideal Store")
  - Нажмите "Create"
  - ⚠️ **Скопируйте токен сразу!** Он показывается только один раз

### Шаг 2: Добавьте переменные окружения на хостинге

**Если используете Railway:**
1. Railway → ваш проект → **Variables**
2. Нажмите **"+ New Variable"**
3. Добавьте:
   - **Key**: `CARDLINK_SHOP_ID`
   - **Value**: `oa2Q9jq2Al` (ваш Shop ID)
4. Добавьте:
   - **Key**: `CARDLINK_API_TOKEN`
   - **Value**: ваш API токен (который скопировали)
5. Нажмите **"Save"**

**Если используете Render:**
1. Render → ваш сервис → **Environment**
2. Нажмите **"Add Environment Variable"**
3. Добавьте те же переменные

### Шаг 3: Настройте Webhook URL в CardLink

1. В личном кабинете CardLink → проект "Benefideal"
2. Перейдите в **Settings** (Настройки)
3. Найдите раздел **"Shop links"** или **"Webhooks"**
4. В поле **"Result URL"** (или "Callback URL", "Postback URL") укажите:

   **Если используете Railway:**
   ```
   https://benefideal-store-production.up.railway.app/api/cardlink/callback
   ```
   
   **Если используете свой домен:**
   ```
   https://benefideal.ru/api/cardlink/callback
   ```

5. Также укажите:
   - **Success URL**: `https://benefideal.ru/payment-success.html`
   - **Fail URL**: `https://benefideal.ru/payment-fail.html`
6. Нажмите **"Update"** или **"Save"**

### Шаг 4: Перезапустите сервер

После добавления переменных окружения:
- Railway: автоматически перезапустится
- Render: нажмите **"Manual Deploy"** → **"Deploy latest commit"**

---

## 🔧 Настройка Enot.io (альтернативная система)

Если хотите использовать Enot.io вместо CardLink:

### Шаг 1: Получите данные из личного кабинета Enot.io

1. Зайдите на https://enot.io
2. Войдите в свой аккаунт
3. Перейдите в раздел **"API"** или **"Настройки"**

**Что нужно скопировать:**

- **API Key** (публичный ключ)
- **Secret Key** (секретный ключ для webhook)

### Шаг 2: Добавьте переменные окружения

На Railway/Render добавьте:
- **Key**: `ENOT_API_KEY`
- **Value**: ваш API ключ

- **Key**: `ENOT_SECRET_KEY`
- **Value**: ваш секретный ключ

### Шаг 3: Настройте Webhook URL в Enot.io

1. В личном кабинете Enot.io → **"Webhooks"**
2. Укажите URL:
   ```
   https://benefideal.ru/api/enot/callback
   ```
   или
   ```
   https://benefideal-store-production.up.railway.app/api/enot/callback
   ```

---

## ✅ Проверка работы

### 1. Проверьте конфигурацию

Откройте в браузере:
```
https://benefideal.ru/api/cardlink/check-config
```

Должно вернуться:
```json
{
  "success": true,
  "configured": true,
  "message": "CardLink настроен правильно"
}
```

### 2. Сделайте тестовый заказ

1. Зайдите на сайт
2. Добавьте товар в корзину
3. Перейдите к оплате
4. Выберите "Оплата картой (Cardlink)"
5. Заполните форму
6. Нажмите "Оплатить"

### 3. Проверьте логи

**Railway:**
- Railway → проект → **Logs**
- Ищите строки:
  - `💳 Creating Cardlink payment`
  - `✅ Cardlink payment created successfully`
  - `📞 Cardlink callback received`
  - `✅ Payment successful`

**Render:**
- Render → сервис → **Logs**
- Те же строки

### 4. Проверьте уведомления

После успешной оплаты должно прийти уведомление в Telegram с информацией о заказе.

---

## ❌ Что делать, если не работает

### Проблема: "Cardlink не настроен"

**Решение:**
- Проверьте, что переменные `CARDLINK_SHOP_ID` и `CARDLINK_API_TOKEN` добавлены на хостинге
- Убедитесь, что значения правильные (не `YOUR_SHOP_ID` или `YOUR_API_TOKEN`)
- Перезапустите сервер

### Проблема: Платеж создается, но деньги не приходят

**Решение:**
- Проверьте, что **Result URL** правильно указан в CardLink
- URL должен быть доступен из интернета (не localhost)
- Должен быть HTTPS (не HTTP)
- Проверьте логи - есть ли запросы на `/api/cardlink/callback`

### Проблема: Callback не приходит

**Решение:**
1. Проверьте URL в CardLink - должен быть точно:
   ```
   https://benefideal.ru/api/cardlink/callback
   ```
2. Убедитесь, что сервер работает (проверьте `/health`)
3. Проверьте логи - CardLink должен отправлять POST запросы
4. Если используете Railway - убедитесь, что домен правильно настроен

### Проблема: Заказы не сохраняются

**Решение:**
- Проверьте логи на наличие ошибок базы данных
- Убедитесь, что база данных доступна для записи
- Проверьте, что таблица `pending_orders` создана

---

## 📞 Поддержка

Если ничего не помогает:

1. **Проверьте документацию CardLink:**
   - В личном кабинете CardLink → "API integration" → "Documentation"

2. **Свяжитесь с поддержкой CardLink:**
   - Email: support@cardlink.link
   - Укажите ваш Shop ID и опишите проблему

3. **Проверьте логи сервера:**
   - Все операции логируются с префиксами 💳, ✅, ❌
   - Ищите ошибки в логах

---

## 🎯 Итого

**Минимально необходимо:**
1. ✅ `CARDLINK_SHOP_ID` в переменных окружения
2. ✅ `CARDLINK_API_TOKEN` в переменных окружения
3. ✅ Result URL настроен в CardLink
4. ✅ Сервер доступен из интернета

После этого платежи должны работать! 💰



