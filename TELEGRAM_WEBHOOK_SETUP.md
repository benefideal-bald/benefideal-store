# Настройка Telegram Webhook для чата поддержки

## ⚠️ ВАЖНО: Webhook обязателен для работы кнопки "Ответить"

Без настроенного webhook кнопка "💬 Ответить" не будет работать!

## Способ 1: Автоматическая настройка (рекомендуется)

После деплоя сервера откройте в браузере:

```
https://ваш-домен.com/api/telegram/set-webhook
```

Или с указанием URL:

```
https://ваш-домен.com/api/telegram/set-webhook?url=https://ваш-домен.com/api/telegram/webhook
```

Вы должны увидеть ответ:
```json
{
  "success": true,
  "message": "Webhook установлен",
  "webhookUrl": "https://ваш-домен.com/api/telegram/webhook"
}
```

## Способ 2: Через BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/setwebhook`
3. Выберите вашего бота
4. Отправьте URL: `https://ваш-домен.com/api/telegram/webhook`

## Проверка webhook

Проверить статус webhook можно через:

```
https://api.telegram.org/bot<ВАШ_BOT_TOKEN>/getWebhookInfo
```

Или через endpoint:

```
https://ваш-домен.com/api/telegram/set-webhook
```

## Как это работает

1. Клиент отправляет сообщение через чат на сайте
2. Сообщение приходит в Telegram с кнопкой "💬 Ответить"
3. **Вы нажимаете кнопку** → Бот должен сразу ответить: "Введите ответ для этого сообщения..."
4. Вы отправляете текстовое сообщение боту (ответом на его сообщение)
5. Ответ автоматически отправляется клиенту в чат на сайте

## Отладка

Если кнопка не работает:

1. **Проверьте webhook:**
   - Откройте: `https://ваш-домен.com/api/telegram/set-webhook`
   - Должен быть ответ с `"success": true`

2. **Проверьте логи сервера:**
   - При нажатии на кнопку должны появиться логи:
     - `📥 Telegram webhook received`
     - `🔘 Callback query received`
     - `✅ Callback query answered`
     - `✅ Reply prompt sent`

3. **Проверьте, что сервер доступен:**
   - Webhook должен быть на HTTPS (не HTTP)
   - Сервер должен быть доступен из интернета
   - URL должен быть публичным (не localhost)

## Важно

- Webhook должен быть настроен на HTTPS URL (не HTTP)
- Сервер должен быть доступен из интернета
- URL должен быть публичным (не localhost)
- После настройки webhook кнопка должна работать сразу

