# Исправление проблемы с удалением базы данных на Render

## Проблема
На Render Free плане файловая система **НЕ персистентна**. Это означает, что файлы могут удаляться при каждом деплое или перезапуске сервера.

## Решение

### 1. База данных в Git (Текущее решение)
База данных `subscriptions.db` теперь коммитится в Git репозиторий. Это гарантирует, что:
- База данных сохраняется между деплоями
- Все отзывы и заказы не теряются
- При каждом деплое база данных восстанавливается из Git

**Важно**: Это временное решение. Для продакшена рекомендуется использовать PostgreSQL.

### 2. Настройки базы данных
- **Путь**: `/opt/render/project/src/subscriptions.db` (на Render) или `./subscriptions.db` (локально)
- **WAL mode**: включен для лучшей производительности
- **Synchronous FULL**: все записи гарантированно сохраняются на диск
- **Автоматический checkpoint**: после каждой операции

### 3. Автоматическое восстановление
- При каждом запуске сервера проверяется наличие всех отзывов
- Если отзыв отсутствует, он автоматически восстанавливается из заказа

## Что делать после деплоя

1. **Проверьте логи Render**:
   - Откройте панель Render
   - Перейдите в "Logs"
   - Найдите строки:
     - `✅ Database opened successfully at: /opt/render/project/src/subscriptions.db`
     - `✅ Database file exists: true`
     - `📊 Reviews count on startup: X`

2. **Проверьте базу данных**:
   - Откройте сайт
   - Перейдите на страницу отзывов
   - Убедитесь, что все отзывы на месте

3. **Если отзывы пропадают**:
   - Проверьте, что база данных в Git (не в .gitignore)
   - Проверьте логи на наличие ошибок
   - Рассмотрите переход на PostgreSQL

## Альтернативные решения (Рекомендуется для продакшена)

### Вариант 1: Render PostgreSQL
1. Создайте PostgreSQL базу данных на Render (есть бесплатный тир)
2. Измените код для использования PostgreSQL вместо SQLite
3. База данных будет полностью персистентной и надежной

### Вариант 2: Railway
Railway имеет персистентное хранилище на Free плане:
1. Зайдите на https://railway.app
2. Создайте новый проект
3. Деплойте из GitHub репозитория
4. База данных будет сохраняться автоматически

### Вариант 3: Render Disk
Render Disk - это платная функция Render для персистентного хранилища:
1. Создайте Render Disk в панели Render
2. Подключите его к вашему сервису
3. Измените путь базы данных на путь к диску

## Текущая конфигурация

- **База данных в Git**: ✅ Да (для сохранения между деплоями)
- **WAL mode**: ✅ Включен
- **Synchronous**: ✅ FULL
- **Автоматический checkpoint**: ✅ После каждой операции
- **Автоматическое восстановление**: ✅ При каждом запуске

## Важно

База данных в Git - это **временное решение**. Для продакшена рекомендуется:
1. Использовать PostgreSQL (Render PostgreSQL или внешний сервис)
2. Или использовать Railway, который имеет персистентное хранилище на Free плане
3. Или использовать Render Disk для персистентного хранилища

