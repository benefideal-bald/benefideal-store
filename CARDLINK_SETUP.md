# Инструкция по настройке CardLink

## Что было сделано

Интеграция CardLink полностью настроена и готова к использованию. Система автоматически:
- Создает платежи через API CardLink
- Сохраняет данные заказов в базу данных
- Обрабатывает webhook от CardLink
- Сохраняет подписки в базу данных
- Отправляет уведомления в Telegram
- Генерирует расписание продлений для ChatGPT, CapCut и Adobe

## Настройка переменных окружения на Render

Для работы CardLink необходимо настроить следующие переменные окружения в панели управления Render:

### Обязательные переменные:

1. **CARDLINK_SHOP_ID** - ID вашего магазина в CardLink
   - **Где найти:**
     1. В личном кабинете CardLink откройте проект "Benefideal"
     2. Перейдите в раздел "API integration" (в левом меню или в настройках проекта)
     3. В разделе "API integration" вы увидите "Your Shop ID:" - это и есть ваш Shop ID
   - **Ваш Shop ID:** `oa2Q9jq2Al` (уже виден на странице)
   - Скопируйте его, нажав на иконку копирования рядом с полем

2. **CARDLINK_API_TOKEN** - API токен для доступа к API CardLink
   - **Где создать:**
     1. На странице "API integration" прокрутите вниз до раздела "Tokens"
     2. Нажмите кнопку **"+ Create"** (справа от заголовка "Tokens")
     3. Введите имя токена (например, "Benefideal Store")
     4. Нажмите "Create" или "Generate"
     5. **Скопируйте токен сразу!** Он показывается только один раз
   - ⚠️ **Важно:** Сохраните токен сразу в безопасном месте, он может не показываться повторно
   - Если токен потерян, нужно будет создать новый

3. **CARDLINK_API_URL** (опционально) - URL API endpoint
   - По умолчанию: `https://cardlink.link/api/v1/bill/create`
   - Если CardLink использует другой URL, укажите его здесь
   - Обычно можно найти в разделе "API integration" → "Documentation"

### Альтернативный вариант (без API):

Если у вас нет доступа к API CardLink или вы хотите использовать платежные ссылки:

1. **CARDLINK_PAYMENT_LINK** - Статическая платежная ссылка
   - Где найти: Личный кабинет CardLink → My Links → Create payment link
   - Пример: `https://cardlink.link/pay/XXXXX`
   - Примечание: В этом случае пользователь должен будет ввести сумму вручную

## Настройка Webhooks (Callback URL)

Webhooks — это способ, которым CardLink уведомляет ваш сервер о статусе платежа. После успешной оплаты CardLink отправляет POST запрос на указанный URL.

### Где настроить Webhook в CardLink:

1. Откройте проект "Benefideal" в личном кабинете CardLink
2. Перейдите в настройки магазина (Shop settings)
3. Найдите раздел **"Shop links"**
4. В поле **"Result URL"** (Postback URL for payment results) должно быть указано:
   ```
   https://benefideal.ru/api/cardlink/callback
   ```
   ⚠️ **Примечание:** Если это поле нельзя изменить, оставьте как есть - endpoint в коде настроен на `/api/cardlink/callback`
5. Также убедитесь, что указаны:
   - **Success URL:** `https://benefideal.ru/payment-success.html`
   - **Fail URL:** `https://benefideal.ru/payment-fail.html`
6. Нажмите кнопку **"Update"** для сохранения

### Как работает Webhook:

1. Пользователь оплачивает заказ через CardLink
2. CardLink обрабатывает платеж
3. CardLink отправляет POST запрос на ваш сервер: `https://benefideal.ru/api/cardlink/callback`
4. Ваш сервер получает данные о платеже и:
   - Сохраняет заказ в базу данных
   - Отправляет уведомление в Telegram
   - Генерирует расписание продлений

### Проверка Webhook:

После настройки вы можете проверить работу webhook:
- В логах сервера на Render ищите сообщения: `📞 Cardlink callback received`
- При успешной оплате должно появиться: `✅ Payment successful`

⚠️ **Важно:** 
- URL должен быть доступен из интернета (не localhost)
- Используйте HTTPS (не HTTP)
- Убедитесь, что домен `benefideal.ru` правильно настроен на Render
- Webhook должен отвечать статусом 200 OK, иначе CardLink может повторять запросы

## Как добавить переменные окружения на Render

1. Войдите в панель управления Render
2. Выберите ваш сервис (Web Service)
3. Перейдите в раздел **Environment**
4. Нажмите **Add Environment Variable**
5. Добавьте каждую переменную:
   - **Key**: `CARDLINK_SHOP_ID`
   - **Value**: ваш Shop ID
6. Повторите для всех необходимых переменных
7. Нажмите **Save Changes**
8. Дождитесь перезапуска сервиса

## Проверка работы

После настройки переменных окружения:

1. Перезапустите сервис на Render
2. Проверьте логи сервера - должны появиться сообщения о создании таблицы `pending_orders`
3. Попробуйте создать тестовый заказ через интерфейс магазина
4. Выберите способ оплаты "Оплата картой (Cardlink)"
5. Проверьте, что:
   - Создается платеж в CardLink
   - Данные заказа сохраняются в базу данных
   - При успешной оплате отправляются уведомления в Telegram

## Структура данных

Система использует следующие таблицы:

- **pending_orders** - временное хранилище заказов до подтверждения платежа
- **subscriptions** - постоянное хранилище подписок после успешной оплаты
- **reminders** - напоминания о продлении подписок

## Обработка платежей

При успешной оплате через CardLink:

1. CardLink отправляет POST запрос на `/api/cardlink/callback`
2. Система получает данные заказа из `pending_orders`
3. Каждый товар сохраняется в `subscriptions`
4. Отправляются уведомления в Telegram
5. Генерируются напоминания о продлении (для ChatGPT, CapCut, Adobe)
6. Заказ помечается как обработанный

## Устранение проблем

### Платеж не создается

- Проверьте, что `CARDLINK_SHOP_ID` и `CARDLINK_API_TOKEN` установлены правильно
- Проверьте логи сервера на наличие ошибок
- Убедитесь, что API URL правильный

### Callback не приходит

- Проверьте, что Result URL указан правильно в личном кабинете CardLink (`https://benefideal.ru/api/cardlink/callback`)
- Убедитесь, что сервер доступен из интернета (не localhost)
- Проверьте логи сервера на наличие входящих запросов

### Заказы не сохраняются

- Проверьте логи сервера на наличие ошибок базы данных
- Убедитесь, что база данных доступна для записи
- Проверьте, что таблица `pending_orders` создана

## Дополнительная информация

- Документация CardLink: проверьте документацию API в личном кабинете CardLink
- Логи сервера: все операции логируются с префиксами 💳, ✅, ❌ для удобства поиска
- Тестирование: рекомендуется сначала протестировать на небольшой сумме

