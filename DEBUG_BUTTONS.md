# 🔍 Отладка: Почему не работают кнопки "Ответить"

## Проблема: Кнопки не отображаются в Telegram

Если кнопки "💬 Ответить" не видны под сообщениями, проверьте следующее:

## 1. Проверьте логи сервера

После отправки сообщения через чат на сайте, проверьте логи Railway:
- Railway → Deployments → последний деплой → Logs
- Ищите: `📤 Sending message with keyboard`
- Должен быть JSON с `inline_keyboard`

**Если этого лога НЕТ** - сообщение не отправляется через `/api/support/send-message`

## 2. Проверьте, что сообщение отправляется правильно

Откройте в браузере (DevTools → Network):
- Отправьте сообщение через чат на сайте
- Проверьте запрос к `/api/support/send-message`
- Должен быть ответ: `{"success": true, "messageId": "..."}`

## 3. Проверьте формат кнопки

Кнопка должна быть в формате:
```json
{
  "inline_keyboard": [[
    {
      "text": "💬 Ответить",
      "callback_data": "reply_support_1234567890_abc123"
    }
  ]]
}
```

## 4. Проверьте webhook

Даже если кнопки видны, они не будут работать без webhook:

1. Откройте: `https://ВАШ_RAILWAY_URL/api/telegram/webhook-status`
2. Должен быть `"url": "https://..."` (не пустой!)
3. Если пустой - настройте webhook: `https://ВАШ_RAILWAY_URL/api/telegram/set-webhook?url=https://ВАШ_RAILWAY_URL/api/telegram/webhook`

## 5. Проверьте, что используется правильный endpoint

Сообщения из чата поддержки должны отправляться через:
- ✅ `/api/support/send-message` - добавляет кнопки
- ❌ `/api/track-visit` - НЕ добавляет кнопки (только уведомления о посетителях)

## 6. Тестовая проверка

1. Откройте чат поддержки на сайте
2. Отправьте тестовое сообщение (например: "тест")
3. Проверьте логи Railway - должны быть:
   - `📤 Sending message with keyboard`
   - `✅ Message sent, response: {...}`
4. В Telegram должно прийти сообщение с кнопкой "💬 Ответить"

## 7. Если кнопки все еще не видны

Проверьте:
- Правильный ли `BOT_TOKEN` в Railway → Variables
- Правильный ли `CHAT_ID` в Railway → Variables
- Работает ли сервер (проверьте `/health`)

## 8. Альтернатива: Ответить без кнопки

Если кнопки не работают, можно просто **ответить на сообщение** (reply) в Telegram - это тоже должно работать.

