# ✅ Проверка статуса платежей

## Что проверить сейчас:

### 1. Проверьте конфигурацию CardLink

Откройте в браузере:
```
https://benefideal.ru/api/cardlink/check-config
```

Должно вернуться:
```json
{
  "success": true,
  "configured": true,
  "message": "CardLink настроен правильно",
  "config": {
    "shop_id_configured": true,
    "api_token_configured": true,
    ...
  }
}
```

### 2. Проверьте Webhook URL в CardLink

⚠️ **ВАЖНО:** Убедитесь, что в CardLink указан правильный Callback URL:

1. Зайдите в CardLink → проект "Benefideal"
2. Settings → "Shop links" или "Webhooks"
3. Проверьте "Result URL" (Callback URL):
   ```
   https://benefideal.ru/api/cardlink/callback
   ```

### 3. Сделайте тестовый заказ

1. Зайдите на сайт: https://benefideal.ru
2. Добавьте товар в корзину
3. Перейдите к оплате
4. Выберите "Оплата картой (Cardlink)"
5. Заполните форму
6. Нажмите "Оплатить"

### 4. Проверьте логи в Railway

Railway → ваш проект → **Logs** → ищите:
- `💳 Creating Cardlink payment` - платеж создается
- `✅ Cardlink payment created successfully` - платеж создан
- `📞 Cardlink callback received` - пришел callback от CardLink
- `✅ Payment successful` - платеж успешен

### 5. Проверьте Telegram

После успешной оплаты должно прийти уведомление в Telegram с информацией о заказе.

---

## 🎯 Если всё настроено правильно:

✅ Переменные окружения в Railway — настроены
✅ API токен CardLink — настроен
✅ Shop ID — настроен

**Осталось только:**
- Проверить Webhook URL в CardLink
- Сделать тестовый заказ
- Проверить логи

---

## ❓ Если что-то не работает:

1. **Проверьте логи Railway** — там будут ошибки
2. **Проверьте Webhook URL** в CardLink
3. **Проверьте конфигурацию** через `/api/cardlink/check-config`



