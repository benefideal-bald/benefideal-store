# 🔑 Где находятся все токены

## 📍 В Railway (где вы сейчас)

Вы находитесь на странице **Settings**. Токены находятся в другом разделе:

### ✅ Правильный путь:

1. В левом меню Railway нажмите **"Variables"** (не Settings!)
2. Или в верхней навигации: **Variables** → там все переменные окружения

**Там должны быть:**

```
✅ CARDLINK_SHOP_ID = oa2Q9jq2Al
✅ CARDLINK_API_TOKEN = (ваш токен из CardLink)
✅ TELEGRAM_BOT_TOKEN = 8460494431:AAFOmSEPrzQ1j4_L-4vBG_c38iL2rfx41us
✅ TELEGRAM_CHAT_ID = 8334777900
✅ ADMIN_PASSWORD = (ваш пароль)
```

---

## 🔍 Где взять токены:

### 1. **CARDLINK_SHOP_ID** 
**Значение:** `oa2Q9jq2Al` (уже известен)

**Где проверить:**
- CardLink → проект "Benefideal" → "API integration"
- Там будет "Your Shop ID: oa2Q9jq2Al"

### 2. **CARDLINK_API_TOKEN** ⚠️ ВАЖНО
**Где получить:**
1. CardLink → проект "Benefideal" → **"API integration"**
2. Прокрутите вниз до раздела **"Tokens"**
3. Нажмите **"+ Create"** (справа от заголовка "Tokens")
4. Введите имя: "Benefideal Store"
5. Нажмите "Create"
6. ⚠️ **Скопируйте токен СРАЗУ!** Он показывается только один раз
7. Вставьте в Railway → Variables → `CARDLINK_API_TOKEN`

**Если токен уже создан, но вы его не помните:**
- Нужно создать новый токен (старый не показывается повторно)
- Или удалить старый и создать новый

### 3. **TELEGRAM_BOT_TOKEN**
**Значение:** `8460494431:AAFOmSEPrzQ1j4_L-4vBG_c38iL2rfx41us` (уже есть в коде)

**Где проверить:**
- Telegram → найдите бота @BotFather
- Отправьте `/mybots`
- Выберите вашего бота
- Там будет токен

### 4. **TELEGRAM_CHAT_ID**
**Значение:** `8334777900` (уже есть в коде)

**Где узнать:**
- Это ваш Telegram ID
- Можно узнать через бота @userinfobot

### 5. **ADMIN_PASSWORD**
**Где установить:**
- Придумайте свой пароль для админ-панели
- Добавьте в Railway → Variables → `ADMIN_PASSWORD`

---

## 🎯 Быстрая инструкция для Railway:

1. **В Railway:** Нажмите **"Variables"** в левом меню (не Settings!)
2. **Проверьте, есть ли:**
   - `CARDLINK_SHOP_ID`
   - `CARDLINK_API_TOKEN`
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
   - `ADMIN_PASSWORD`

3. **Если чего-то нет:**
   - Нажмите **"+ New Variable"**
   - Введите **Key** и **Value**
   - Нажмите **"Add"**

4. **После добавления:** Railway автоматически перезапустит сервер

---

## 📋 Полный список всех токенов:

### Обязательные для платежей:
- ✅ `CARDLINK_SHOP_ID` = `oa2Q9jq2Al`
- ✅ `CARDLINK_API_TOKEN` = (из CardLink → API integration → Tokens)

### Для Telegram уведомлений:
- ✅ `TELEGRAM_BOT_TOKEN` = `8460494431:AAFOmSEPrzQ1j4_L-4vBG_c38iL2rfx41us`
- ✅ `TELEGRAM_CHAT_ID` = `8334777900`

### Для админ-панели:
- ✅ `ADMIN_PASSWORD` = (ваш пароль)

### Опциональные (если используете Enot.io):
- `ENOT_API_KEY` = (из enot.io)
- `ENOT_SECRET_KEY` = (из enot.io)

---

## 🔗 Полезные ссылки:

- **CardLink API:** https://cardlink.link → проект "Benefideal" → "API integration"
- **Railway Variables:** Railway → ваш проект → **Variables**
- **Telegram Bot:** @BotFather в Telegram

---

## ❓ Если токен не работает:

1. **Проверьте в Railway:**
   - Railway → Variables → убедитесь, что токен правильно скопирован (без пробелов)

2. **Проверьте в CardLink:**
   - CardLink → API integration → Tokens
   - Если токен не виден, создайте новый

3. **Перезапустите сервер:**
   - Railway автоматически перезапускается при изменении Variables
   - Или вручную: Railway → Deployments → "Redeploy"



