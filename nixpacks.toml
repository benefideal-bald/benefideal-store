[phases.setup]
nixPkgs = ["nodejs-18_x", "python3", "make", "gcc"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "node server.js"

