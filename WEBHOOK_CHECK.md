# Проверка и настройка Telegram Webhook

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Без webhook кнопка "Ответить" НЕ РАБОТАЕТ!

## Шаг 1: Проверьте текущий статус webhook

Откройте в браузере:
```
https://ваш-домен.com/api/telegram/webhook-status
```

Вы должны увидеть что-то вроде:
```json
{
  "success": true,
  "webhookInfo": {
    "ok": true,
    "result": {
      "url": "https://ваш-домен.com/api/telegram/webhook",
      "has_custom_certificate": false,
      "pending_update_count": 0
    }
  }
}
```

**Если `url` пустой или неправильный** - webhook НЕ настроен!

## Шаг 2: Настройте webhook

Откройте в браузере:
```
https://ваш-домен.com/api/telegram/set-webhook
```

Вы должны увидеть:
```json
{
  "success": true,
  "message": "Webhook установлен",
  "webhookUrl": "https://ваш-домен.com/api/telegram/webhook"
}
```

## Шаг 3: Проверьте логи сервера

После настройки webhook:
1. Отправьте тестовое сообщение через чат на сайте
2. В Telegram должно прийти сообщение с кнопкой "💬 Ответить"
3. **Нажмите на кнопку**
4. Проверьте логи сервера - должны появиться:
   - `📥 Telegram webhook received`
   - `🔘 Callback query received`
   - `💬 Processing reply button click`
   - `✅ Callback query answered`
   - `✅ Reply prompt sent`

**Если этих логов НЕТ** - webhook не работает или не настроен!

## Шаг 4: Проверьте, что сервер доступен

Webhook должен быть доступен по HTTPS:
- ✅ `https://ваш-домен.com/api/telegram/webhook`
- ❌ `http://ваш-домен.com/api/telegram/webhook` (не работает!)
- ❌ `http://localhost:3000/api/telegram/webhook` (не работает!)

## Что должно происходить при нажатии на кнопку:

1. Вы нажимаете кнопку "💬 Ответить" в Telegram
2. Бот сразу отвечает: "Введите ваш ответ следующим сообщением"
3. Бот отправляет сообщение: "💬 Введите ответ для этого сообщения..."
4. Вы отвечаете на это сообщение (reply) с текстом ответа
5. Ответ отправляется клиенту в чат на сайте

## Если кнопка все еще не работает:

1. **Проверьте webhook статус** - `/api/telegram/webhook-status`
2. **Настройте webhook** - `/api/telegram/set-webhook`
3. **Проверьте логи** - должны быть логи при нажатии на кнопку
4. **Проверьте, что сервер доступен** - должен быть HTTPS
5. **Попробуйте просто ответить на сообщение** (reply) - это тоже должно работать

## Альтернативный способ (без кнопки):

Если кнопка не работает, вы можете просто **ответить на сообщение** (reply) в Telegram - это тоже будет работать и отправит ответ клиенту.

