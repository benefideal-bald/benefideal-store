# 🔧 Решение: Нельзя изменить Result URL в CardLink

## Проблема

В CardLink нельзя изменить **Result URL** в настройках магазина, но нужно, чтобы webhook работал с Railway.

## ✅ Решение

### Вариант 1: Проверьте раздел "API Integration" в CardLink

В CardLink может быть отдельная настройка для Webhook:

1. Зайдите в CardLink → проект "Benefideal"
2. Найдите раздел **"API Integration"** (в левом меню или в настройках)
3. Проверьте, есть ли там поле **"Webhook URL"** или **"Callback URL"**
4. Если есть — измените на:
   ```
   https://benefideal-store-production.up.railway.app/api/cardlink/callback
   ```

### Вариант 2: Измените endpoint в коде (если Result URL нельзя изменить)

Если Result URL в CardLink указывает на другой URL (например, старый Render), можно изменить endpoint в коде:

**Текущий endpoint:** `/api/cardlink/callback`

**Если в CardLink указан другой путь**, можно:
1. Создать новый endpoint, который соответствует тому, что указано в CardLink
2. Или изменить существующий endpoint

**НО:** Лучше найти, где в CardLink можно изменить Webhook URL.

---

## 🔍 Где искать Webhook настройки в CardLink

1. **API Integration** — основной раздел для API настроек
2. **Webhooks** — отдельный раздел (если есть)
3. **Settings** → **Advanced** — дополнительные настройки
4. **Project Settings** — настройки проекта

---

## ⚠️ Важно

- **Result URL** и **Webhook URL** могут быть разными настройками
- Result URL — куда перенаправляется пользователь после оплаты
- Webhook URL — куда CardLink отправляет уведомления о статусе платежа

---

## 📞 Если ничего не помогает

Свяжитесь с поддержкой CardLink и попросите:
- Изменить Webhook URL на `https://benefideal-store-production.up.railway.app/api/cardlink/callback`
- Или объяснить, где в интерфейсе можно изменить Webhook URL

---

## 🎯 Проверка

После настройки:
1. Сделайте тестовый заказ
2. Проверьте логи Railway → Logs
3. Должны увидеть: `📞 Cardlink callback received`

